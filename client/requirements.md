## Packages
framer-motion | Page transitions and scroll-triggered animations to give the app a premium mobile feel

## Notes
- Tailwind configuration assumption: The frontend is generated with PWA/mobile-first styles.
- The layout is intentionally restricted to `max-w-md mx-auto` to simulate a mobile app interface on desktop while remaining fully usable.
- Font family `Kanit` is used for a modern Thai typography aesthetic.
